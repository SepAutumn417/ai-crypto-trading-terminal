{
  "name": "crypto-terminal",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "build:web": "pnpm --filter web build"
  },
  "pnpm": {
    "overrides": {
      "minimatch": "^9.0.7",
      "postcss": "^8.5.10",
      "glob": "^10.5.0"
    }
  }
}
