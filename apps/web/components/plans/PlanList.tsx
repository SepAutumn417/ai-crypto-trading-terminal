'use client';
import type { TradePlan } from '@/lib/api';
import { cn } from '@/lib/utils';

const statusStyles:Record<string,string>={DRAFT:'bg-slate-700 text-slate-300',CHECKED:'bg-blue-500/15 text-blue-300',READY_FOR_CONFIRMATION:'bg-emerald-500/15 text-emerald-300',SUBMITTED:'bg-amber-500/15 text-amber-300',PARTIALLY_FILLED:'bg-orange-500/15 text-orange-300',FILLED:'bg-emerald-500/15 text-emerald-300',FAILED:'bg-rose-500/15 text-rose-300',EXPIRED:'bg-slate-800 text-slate-500',CANCELLED:'bg-slate-800 text-slate-500'};
function formatDate(iso:string){const d=new Date(iso);return `${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')} ${String(d.getHours()).padStart(2,'0')}:${String(d.getMinutes()).padStart(2,'0')}`;}
export function PlanList({plans,loading,onSelect,selectedId}:{plans:TradePlan[];loading:boolean;onSelect:(id:string)=>void;selectedId:string|null}) { if(loading)return <p className="py-3 text-sm text-slate-500">正在加载计划…</p>; if(plans.length===0)return <div className="py-4 text-sm text-slate-500">暂无已保存计划。创建后会出现在这里。</div>; return <div className="space-y-2">{plans.map((plan)=><button key={plan.id} onClick={()=>onSelect(plan.id)} className={cn('w-full rounded-lg border p-3.5 text-left transition',selectedId===plan.id?'border-blue-400/60 bg-blue-500/10':'border-slate-800 bg-slate-950/20 hover:border-slate-700 hover:bg-slate-900/60')}><div className="flex items-center justify-between gap-3"><div className="flex items-center gap-2"><span className={cn('rounded px-1.5 py-0.5 text-[11px] font-semibold',plan.direction==='LONG'?'bg-emerald-500/15 text-emerald-300':'bg-rose-500/15 text-rose-300')}>{plan.direction==='LONG'?'做多':'做空'}</span><span className="text-sm font-semibold text-slate-100">{plan.symbol}</span></div><span className={cn('rounded-full px-2 py-0.5 text-[10px] font-medium',statusStyles[plan.status]||statusStyles.DRAFT)}>{plan.status}</span></div><div className="mt-3 grid grid-cols-3 gap-2 text-xs"><span className="text-slate-500">入场 <b className="ml-1 font-medium text-slate-300">{plan.entry_price}</b></span><span className="text-slate-500">风险 <b className="ml-1 font-medium text-slate-300">{plan.risk_percent}%</b></span><span className="text-right text-slate-600">{formatDate(plan.created_at)}</span></div></button>)}</div>; }
